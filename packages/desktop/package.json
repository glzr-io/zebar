{
  "name": "@zebar/desktop",
  "version": "0.0.0",
  "scripts": {
    "dev": "npm run tauri dev -- -- -- open window/bar",
    "dev:wip": "npm run -s monitors -- --print0 | xargs -0 -P 99 -I % sh -c 'cargo run open window/bar --args %'",
    "build": "npm run tauri build",
    "monitors": "cargo run --no-default-features --quiet -- monitors",
    "tauri": "tauri"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "2.0.0-alpha.17",
    "typescript": "5.0.4"
  }
}
