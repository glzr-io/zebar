{
  "name": "@zebar/desktop",
  "version": "0.0.0",
  "scripts": {
    "build": "npm run tauri build -- --verbose",
    "dev": "npm run tauri dev -- -- -- open-all",
    "format": "cargo fmt",
    "lint": "cargo fmt --check",
    "tauri": "tauri"
  },
  "dependencies": {},
  "devDependencies": {
    "@tauri-apps/cli": "2.0.0-beta.22",
    "@zebar/client": "workspace:*",
    "typescript": "5.3.3"
  }
}
