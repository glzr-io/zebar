{
  "name": "@zebar/systray-util",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint:check": "cargo clippy --target x86_64-pc-windows-gnu --no-deps --manifest-path ./Cargo.toml",
    "lint:fix": "cargo clippy --fix --target x86_64-pc-windows-gnu --manifest-path ./Cargo.toml"
  }
}
